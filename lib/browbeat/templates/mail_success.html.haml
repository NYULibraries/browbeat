%p The following applications previously had failures but are now operational:
%ul
  - application_list.each do |application|
    - next unless previously_failing?(application) || previously_failing_on_staging?(application)
    %li
      = application.name
      = "(staging-only outage)" unless previously_failing?(application)
%p The following applications remained operational:
%ul
  - application_list.each do |application|
    - next if previously_failing?(application) || previously_failing_on_staging?(application)
    %li= application.name
